[project]
name = "finance-api"
version = "0.1.0"
description = "API de finanzas personales: ingresos, gastos, activos, deudas y patrimonio en el tiempo"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "psycopg[binary]>=3.2",          # solo para Alembic (motor sincrono)
    "pydantic>=2.10",
    "pydantic-settings>=2.6",
    "email-validator>=2.2",
    "pwdlib[argon2]>=0.2.1",
    "pyjwt>=2.10",
    "python-multipart>=0.0.20",
    "pgvector>=0.3.6",
]

[project.optional-dependencies]
ai = [
    "anthropic>=0.40",
    "voyageai>=0.3",
]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.28",
    "ruff>=0.8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Un solo event loop para toda la corrida. Con el default (uno por prueba), el
# engine de alcance sesion queda atado al loop del primer test y todos los
# demas fallan con InterfaceError al reusar sus conexiones.
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
# Sin esto, ruff toma `alembic` por paquete propio porque existe el directorio
# alembic/ en la raiz, y lo mueve al bloque de imports locales.
known-third-party = ["alembic"]
